import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';

// Simple placeholder test to verify Jest is working
describe('Sanity Check', () => {
  it('true should be true', () => {
    expect(true).toBe(true);
  });
});
